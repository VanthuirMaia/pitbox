name: pitbox
description: Controle de operação para motoristas por app
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  geolocator: ^13.0.0
  workmanager: ^0.6.0
  sqflite: ^2.3.0
  path: ^1.9.0
  go_router: ^14.0.0
  intl: ^0.19.0
  shared_preferences: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
